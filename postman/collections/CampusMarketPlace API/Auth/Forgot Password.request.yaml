$kind: http-request
name: Forgot Password
method: POST
url: '{{baseUrl}}/forgot-password'
auth:
  type: noauth
body:
  type: json
  content: |-
    {
      "email": "john@example.com"
    }
order: 4000
