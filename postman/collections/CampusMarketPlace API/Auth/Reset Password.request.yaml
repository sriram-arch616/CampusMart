$kind: http-request
name: Reset Password
method: POST
url: '{{baseUrl}}/reset-password'
auth:
  type: noauth
body:
  type: json
  content: |-
    {
      "email": "john@example.com",
      "otp": "123456",
      "newPassword": "NewPassword@123"
    }
order: 5000
